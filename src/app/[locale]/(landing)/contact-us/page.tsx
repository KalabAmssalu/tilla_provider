import ContactPage from "@/components/screen/landing/ContactScreen";

type Props = {};

const Carrierpage = (props: Props) => {
	return (
		<div>
			{/* carriers */}
			<ContactPage />
		</div>
	);
};

export default Carrierpage;
