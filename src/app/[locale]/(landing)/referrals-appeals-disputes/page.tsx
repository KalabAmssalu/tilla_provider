import ReferralScreen from "@/components/screen/landing/ReferralScreen";

type Props = {};

const Referralpage = (props: Props) => {
	return (
		<div>
			{/* referral */}
			<ReferralScreen />
		</div>
	);
};

export default Referralpage;
