import AppealSubmit from "@/components/screen/claims/AppealSubmit";

type Props = {};

const Disputpage = (props: Props) => {
	return (
		<div>
			<AppealSubmit />
		</div>
	);
};

export default Disputpage;
