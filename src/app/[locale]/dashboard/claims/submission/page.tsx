import ClaimScreen from "@/components/screen/claims/ClaimScreen";

type Props = {};

const submissionPage = (props: Props) => {
	return (
		<div className="">
			<ClaimScreen />
		</div>
	);
};

export default submissionPage;
