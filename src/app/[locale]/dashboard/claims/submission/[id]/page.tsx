import ClaimSubmit from "@/components/screen/claims/ClaimSubmit";

type Props = {};

const Submissionpage = (props: Props) => {
	return (
		<div>
			<ClaimSubmit />
		</div>
	);
};

export default Submissionpage;
