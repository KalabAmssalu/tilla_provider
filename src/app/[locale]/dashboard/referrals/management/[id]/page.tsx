import ReferralSubmit from "@/components/screen/referrals/ReferralSubmit";

type Props = {};

const Submissionpage = (props: Props) => {
	return (
		<div>
			<ReferralSubmit />
		</div>
	);
};

export default Submissionpage;
