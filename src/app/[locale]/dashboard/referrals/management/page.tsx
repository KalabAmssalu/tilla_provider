import ReferralScreen from "@/components/screen/referrals/ReferralScreen";

type Props = {};

const managementPage = (props: Props) => {
	return (
		<div>
			<ReferralScreen />
		</div>
	);
};

export default managementPage;
