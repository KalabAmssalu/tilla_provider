import PriorAuthorizationScreen from "@/components/screen/referrals/PriorAuthorizationScreen";

type Props = {};

const authorizationpage = (props: Props) => {
	return (
		<div>
			<PriorAuthorizationScreen />
		</div>
	);
};

export default authorizationpage;
